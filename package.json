{
    "scripts": {
        "server": "json-server --watch db.json --port 5500"
    },
    "devDependencies": {
        "json-server": "^1.0.0-beta.3"
    },
    "dependencies": {
        "axios": "^1.10.0"
    }
}
